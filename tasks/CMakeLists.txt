# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(marker_based_slamTaskLib)
ADD_LIBRARY(${MARKER_BASED_SLAM_TASKLIB_NAME} SHARED 
    ${MARKER_BASED_SLAM_TASKLIB_SOURCES})
add_dependencies(${MARKER_BASED_SLAM_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${MARKER_BASED_SLAM_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${MARKER_BASED_SLAM_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${MARKER_BASED_SLAM_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${MARKER_BASED_SLAM_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${MARKER_BASED_SLAM_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${MARKER_BASED_SLAM_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${MARKER_BASED_SLAM_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${MARKER_BASED_SLAM_TASKLIB_HEADERS}
    DESTINATION include/orocos/marker_based_slam)

